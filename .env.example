# Flip to true at P4 (live mode + Stripe Tax enabled) to expose the buy flow.
NEXT_PUBLIC_PAYWALL_LIVE=false

# Cloudflare Worker URL. Use the local wrangler dev URL during development.
NEXT_PUBLIC_WORKER_URL=https://plainvoice-pay.yvendive.workers.dev
